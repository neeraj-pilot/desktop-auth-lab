name: desktop_auth_lab
description: Desktop utility for testing Ente local authentication assumptions.
publish_to: "none"
version: 0.2.0+1

environment:
  sdk: ^3.10.9

dependencies:
  flutter:
    sdk: flutter
  local_auth: 3.0.1
  local_auth_linux:
    path: packages/local_auth_linux

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/polkit/io.ente.auth.policy
